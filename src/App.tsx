import { StyledApp } from "./style";
import { Header } from "components";
import { Form } from "components";

export const App = () => {
  return (
    <StyledApp>
      <Header />
      <Form />
    </StyledApp>
  );
};
